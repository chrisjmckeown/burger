<li>
	{{burger_name}}

	<button class="change-devoured" data-id="{{id}}" data-changedevoured="{{devoured}}">
		{{#if devoured}}DEVOURED!{{else}}EAT ME!{{/if}}
	</button>

	<button class="delete" data-id="{{id}}">
		DELETE
	</button>
</li>
